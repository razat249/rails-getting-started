<div class="login">
	<div class="container">
		<div class="form">

      <h1>Search Artists</h1>

      <%= form_tag({controller: "last_fm", action: "search"}, method: "get", class: "nifty_form") do %>
        <%= label_tag(:q, "Search for:") %>
        <%= text_field_tag(:q) %>
        <%= submit_tag("Search") %>
      <% end %>

    </div>
	</div>
</div>
